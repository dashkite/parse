# Testing

The following information describes the testing procedures for the repository.

## Running Tests

We use a standard test suite to verify the parser combinators. Run the tests by executing `npx genie test` in the terminal.

Tests are located in the `test/` directory and ensure that error handling, backtracking, and state management behave correctly under various parsing conditions.
